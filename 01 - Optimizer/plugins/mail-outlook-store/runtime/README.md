# mail-outlook-store runtime

This directory is the plugin-local runtime boundary for `.pst` and `.ost`
extraction. The generated `python/`, `vendor/`, and `wheelhouse/` contents are
materialized by `bootstrap.py` or `install.ps1`; do not hand-edit them.

`install_state.json` is runtime/build evidence and may be regenerated by the
bootstrap flow.
