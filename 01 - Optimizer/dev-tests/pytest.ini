[pytest]
testpaths = tests
norecursedirs = .pytest_cache .pytest-local-tmp pytest-cache-files-* output state __pycache__
filterwarnings =
    error::pytest.PytestUnhandledThreadExceptionWarning
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
