# Runtime

Reserved for the module-local runtime.

This scaffold intentionally contains no runtime payload. The runtime phase must
define build, check, lockfile, local dependency source and preflight behavior
before any Kernel action is exposed.

