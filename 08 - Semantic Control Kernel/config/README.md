# Config

Reserved for module-local configuration.

No configuration keys are defined in the scaffold. Later phases must classify
each config file as authoring truth, runtime truth or compatibility artifact
before adding it here.

