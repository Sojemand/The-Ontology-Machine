from __future__ import annotations

from tests.fixtures.taxonomy_refactor_classification import (
    classify_path,
    inventory_location_key_for,
    inventory_location_value_for,
    is_text_path,
    origin_bucket_for,
    target_source_for,
)
from tests.fixtures.taxonomy_refactor_io import (
    canonical_json,
    canonical_sha256,
    inventory_file_name,
    live_snapshot_payloads,
    load_cleanup_manifest,
    load_contract_fingerprint_manifest,
    load_contract_inventory_entries,
    load_contract_snapshot_payload,
    load_fingerprint_manifest,
    load_inventory_entries,
    load_json,
    load_provenance,
    load_snapshot_payload,
    normalize_phase0_comparison_payload,
    normalize_phase0_release_payload,
    normalize_phase0_runtime_payload,
    normalize_release_payload,
)
from tests.fixtures.taxonomy_refactor_leafs import (
    build_inventory_entries,
    comparable_leaf_values,
    comparable_snapshot_fingerprint,
    core_leaf_values,
)
from tests.fixtures.taxonomy_refactor_paths import (
    ACTIVE_RELEASE_SNAPSHOT_NAME,
    BASELINE_ROOT,
    CLEANUP_MANIFEST_PATH,
    CONFIG_ROOT,
    CONTRACT_ARTIFACT_KINDS,
    CONTRACT_FINGERPRINT_MANIFEST_PATH,
    CONTRACT_INVENTORY_CASES,
    CONTRACT_INVENTORY_ROOT,
    CONTRACT_RELEASE_SNAPSHOT_NAME,
    CONTRACT_RUNTIME_SNAPSHOT_NAME,
    CONTRACT_SNAPSHOT_ROOT,
    CORE_CLASSIFICATIONS,
    FINGERPRINT_MANIFEST_PATH,
    FROZEN_BASELINE_ROOT,
    INVENTORY_CASES,
    INVENTORY_ROOT,
    MASTER_SNAPSHOT_NAME,
    NORMALIZED_SNAPSHOT_NAME,
    PROJECTION_CATALOG_SNAPSHOT_NAME,
    PROJECTION_SNAPSHOT_NAMES,
    PROJECT_ROOT,
    PROVENANCE_PATH,
    RECIPE_SNAPSHOT_NAME,
    RELEASE_SNAPSHOT_NAME,
    REQUEST_SNAPSHOT_NAME,
    RUNTIME_SNAPSHOT_NAME,
    SEMANTIC_STATUS_SNAPSHOT_NAME,
    SNAPSHOT_ROOT,
    STRUCTURED_SNAPSHOT_NAME,
    VALID_CLASSIFICATIONS,
    projection_id_from_file_name,
)

__all__ = [name for name in globals() if not name.startswith("__")]
