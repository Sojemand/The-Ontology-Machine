[pytest]
addopts = --import-mode=importlib
markers =
    integration: real owner subprocess integration tests with isolated module roots
    gated: integration tests that require explicit external runtime or credential opt-in
